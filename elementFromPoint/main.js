// Your JavaScript code goes here
let pos;

document.addEventListener('mousemove', ({ pageX, pageY }) => {
  pos = `(${pageX}, ${pageY})`;
  //   console.log(pos);
});

// elementFfromPoint or elementsFromPoint
